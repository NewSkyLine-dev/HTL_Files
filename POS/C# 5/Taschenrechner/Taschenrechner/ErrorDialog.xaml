<Window x:Class="Taschenrechner.ErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fehler in der Eingabe"
        Height="300"
        Width="500"
        MinHeight="200"
        MinWidth="350"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   x:Name="ErrorTitle"
                   Text="Es wurden Fehler in der Eingabe gefunden:"
                   Margin="0,0,0,10"
                   FontWeight="SemiBold"
                   Foreground="DarkRed"/>

        <!-- Eingabe mit hervorgehobenen Fehlern -->
        <Border Grid.Row="1"
                BorderBrush="Gray"
                BorderThickness="1"
                Background="#FFF8F8"
                Padding="10"
                Margin="0,0,0,10">
            <RichTextBox x:Name="InputDisplay"
                         IsReadOnly="True"
                         BorderThickness="0"
                         Background="Transparent"
                         FontFamily="Consolas"
                         FontSize="14"/>
        </Border>

        <!-- Fehlerliste -->
        <ListBox Grid.Row="2"
                 x:Name="ErrorList"
                 Margin="0,0,0,15"
                 FontFamily="Consolas">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"
                            Foreground="DarkRed"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Grid.Row="3"
                Content="OK"
                Width="100"
                Height="30"
                HorizontalAlignment="Right"
                Click="OkButton_Click"
                IsDefault="True"/>
    </Grid>
</Window>
